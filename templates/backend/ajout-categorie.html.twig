{% extends 'baseBackend.html.twig' %}
{% block title %}
	{{parent()}}
	Liste thèmes
{% endblock %}
{% block stylesheets %}
			{{parent()}}
{% endblock %}
{% block stylesheetsspan %}
			{{parent()}}
{% endblock %}
{% block contenu %}
	{{parent()}}
	<div class="container-fluid">
		<div class="row justify-content-center">
			<h1 class="text-center text-primary p-4">
				Gestion des catégories
			</h1>

		</div>

		<div class="row justify-content-center">
			<div class="col-8 p-4 m-0 text-primary">
				{{ form_start(form) }}
				<!-- Blog Entries Column -->

				<!-- Title -->
					<div class="form-group text-center col-lg-12 mx-auto" style="max-width:400px"> 
					{{ form_label(form.libelle, 'Nom de la catégorie ', {'label_attr': {'class': 'font-weightbold'}}
					) 
				}}
					{{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}
					) 
				}}
				</div>

				


				<div class="form-group text-center">
					{{ form_widget(form.ajoutercategorie, { 'label': "Envoyer", 'attr': {'class': 'btn'}}
					) }}
				</div>


				{{ form_end(form) }}
			</div>
		</div>
	</div>


{% endblock %}
