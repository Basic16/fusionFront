{% extends 'CocoricoCoreBundle:Dashboard/Listing:layout.html.twig' %}

{% trans_default_domain 'cocorico_listing' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block nav_side_dashboard -%}
    {% set nav_side_dashboard_current = 'mariages' %}
    {{ parent() }}
{%- endblock %}

{% block content_dashboard -%}
    <div class="form-area">
        {{ render(controller('CocoricoCoreBundle:Dashboard/Offerer/Listing:completionNotice', {'listing': listing})) }}

        {% include 'CocoricoCoreBundle:Frontend/Common:_flash.html.twig' %}

        <div class="tab-content">
            <div role="tabpanel" class="tab-pane active" id="info">

                <div class="form-holder" style="min-height: 600px">
                    <h2>Sélectionnez les types de mariages</h2>

                    
       <form method="post" >      
       {%- for unMariage in mariages -%}

               <div class="p-2 m-2">
               <div class="form-check p-2 m-2" style="padding: 10px">
                  <input class="form-check-input m-2 p-2" type="checkbox" value="{{unMariage.id}}" name="mariage[]" id="defaultCheck-{{unMariage.id}}" {% if unMariage in listing.mariages%} checked{% endif %}>
                  <label class="form-check-label m-2 p-2" for="defaultCheck-{{unMariage.id}}">
                  {{unMariage.nom}}
                  </label>
                </div>
                </div>
        {%- endfor -%}
        <div class="btn-block text-center">
          <button type="submit" class="btn btn-default" id="btModifMariages">Modifier</button>
        </div>
        </form>
    </div>

    
    

    
                <!-- form-holder -->
            </div>
        </div>
        <!-- tab-content -->
    </div>
{%- endblock %}


{% block javascripts %}
    {{ parent() }}
    <script>
        $(function () {

        });
    </script>
{% endblock %}